---
topic: B2
title: "SparkSession and Entry Points"
status: complete
chapter: 4
repos: [apache/spark]
configs:
  - spark.app.name
  - spark.master
  - spark.log.level
  - spark.sql.catalogImplementation
  - spark.sql.warehouse.dir
  - spark.sql.session.timeZone
  - spark.pyspark.python
  - spark.pyspark.driver.python
  - spark.python.worker.reuse
  - spark.submit.deployMode
  - spark.executor.isolatedSessionCache.size
  - spark.executor.pyspark.memory
  - spark.api.mode
sources:
  - subsystem: sql/api
    concepts: [SparkSession abstract class, SparkSessionCompanion, SparkSessionBuilder, activeThreadSession, defaultSession]
  - subsystem: sql/core
    concepts: [SparkSession classic implementation, Builder.build, getOrCreate, create, newSession, cloneSession, applyModifiableSettings, SharedState, SessionState]
  - subsystem: core
    concepts: [SparkConf, SparkContext, setLogLevel, local mode scheduler selection, SPARK_LOG_LEVEL config]
  - subsystem: python
    concepts: [PySpark SparkSession, Builder.getOrCreate, _create_shell_session, _getActiveSessionOrCreate, shell.py REPL bootstrap]
---

## What it is

`SparkSession` is the single entry point to Spark's DataFrame and Dataset API. It wraps a `SparkContext` (which owns the cluster connection and scheduler) inside a richer SQL-aware layer that also holds a `SharedState` (warehouse, cache, external catalog — shared across sessions on the same JVM) and a `SessionState` (SQL config, temp views, UDFs, parser — isolated per session). The fluent `Builder` API collects configuration before constructing or reusing a session; two class-level slots — a thread-local active session and a global default session — govern reuse and isolation throughout the application lifetime.

## Code path

```
SparkSession.builder.master(...).appName(...).getOrCreate()
  → Builder.config() / appName() / master()              [sql/api/.../SparkSession.scala:1109]
      accumulates options in HashMap[String, String]
  → Builder.getOrCreate() → build(forceCreate=false)     [sql/core/.../classic/SparkSession.scala:1035]
      1. check activeThreadSession (thread-local)
      2. check defaultSession (AtomicReference) — if found, applyModifiableSettings + return
      3. SparkContext.getOrCreate(sparkConf)              [SparkContext.scala:86]
           → createTaskScheduler(sc, master)             [SparkContext.scala:3308]
                "local"     → 1-thread LocalSchedulerBackend
                "local[N]"  → N threads (N or availableProcessors())
                "local[N,M]"→ N threads, M max task failures
           reads spark.log.level → setLogLevel()         [SparkContext.scala:411]
      4. new SparkSession(sc) → setDefaultAndActiveSession

--- REPL / notebook path ---

pyspark shell.py:82
  → SparkSession._create_shell_session()                 [session.py:1230]
      tries enableHiveSupport(); falls back to _getActiveSessionOrCreate()
  → binds globals: spark, sc, sql, sqlContext

--- PySpark builder path ---

pyspark Builder.getOrCreate()                            [session.py:431]
  checks spark.api.mode / SPARK_REMOTE → Classic or Connect
  Classic: SparkContext.getOrCreate(sparkConf)
         → SparkSession(sc, options=self._options)       [session.py:619]
              jvm.org.apache.spark.sql.classic.SparkSession(jsc.sc(), options)  ← Py4J
```

## Key source locations

- [sql/api/.../SparkSession.scala:63](https://github.com/apache/spark/blob/v4.1.2/sql/api/src/main/scala/org/apache/spark/sql/SparkSession.scala#L63) — abstract `SparkSession` base class; `withActive`, `stop`, `time`
- [sql/api/.../SparkSession.scala:958](https://github.com/apache/spark/blob/v4.1.2/sql/api/src/main/scala/org/apache/spark/sql/SparkSession.scala#L958) — `SparkSessionCompanion`: `activeThreadSession` (InheritableThreadLocal) and `defaultSession` (AtomicReference)
- [sql/api/.../SparkSession.scala:1096](https://github.com/apache/spark/blob/v4.1.2/sql/api/src/main/scala/org/apache/spark/sql/SparkSession.scala#L1096) — abstract `SparkSessionBuilder`; `appName`, `master`, `enableHiveSupport`, `config`, `getOrCreate`, `create`
- [sql/core/.../classic/SparkSession.scala:92](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L92) — concrete `SparkSession` constructor; `SharedState` (lazy), `SessionState` (lazy)
- [sql/core/.../classic/SparkSession.scala:110](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L110) — PySpark-specific constructor; calls `applyAndLoadExtensions`
- [sql/core/.../classic/SparkSession.scala:237](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L237) — `newSession()`: shares `SharedState`, fresh `SessionState`
- [sql/core/.../classic/SparkSession.scala:259](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L259) — `cloneSession()`: copies `SessionState` and `managedJobTags`; forces eager init
- [sql/core/.../classic/SparkSession.scala:979](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L979) — `build(forceCreate)`: thread-local → global → new-create priority
- [sql/core/.../classic/SparkSession.scala:1035](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L1035) — `getOrCreate()` and `create()` thin wrappers
- [sql/core/.../classic/SparkSession.scala:1067](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L1067) — `applyModifiableSettings`: skips static configs, warns on non-modifiable ones
- [sql/core/.../classic/SparkSession.scala:1144](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L1144) — `sessionStateClassName`: `HiveSessionStateBuilder` vs `SessionStateBuilder` based on `spark.sql.catalogImplementation`
- [core/.../SparkConf.scala:284](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/SparkConf.scala#L284) — `SparkConf`; `loadFromSystemProperties`, `set`, `setAll`, `setIfMissing`, `getOption`, `clone`, `validateSettings`
- [core/.../SparkContext.scala:397](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/SparkContext.scala#L397) — `setLogLevel`: validates, calls `Utils.setLogLevelIfNeeded`, optionally syncs to executors
- [core/.../SparkContext.scala:411](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/SparkContext.scala#L411) — startup: reads `spark.log.level` and calls `setLogLevel` before anything else
- [core/.../SparkContext.scala:3308](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/SparkContext.scala#L3308) — `createTaskScheduler`: master URL dispatch; `local` / `local[N]` / `local[*]` / `local[N,M]`
- [core/.../SparkContext.scala:3451](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/SparkContext.scala#L3451) — `SparkMasterRegex` object; `LOCAL_N_REGEX`, `LOCAL_N_FAILURES_REGEX`
- [core/.../internal/config/package.scala:1250](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/internal/config/package.scala#L1250) — `SPARK_LOG_LEVEL` config entry; validates against `VALID_LOG_LEVELS`
- [core/.../util/Utils.scala:2344](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/util/Utils.scala#L2344) — `setLogLevel(Level)` modifies the Log4j2 root logger
- [sql/catalyst/.../internal/StaticSQLConf.scala:35](https://github.com/apache/spark/blob/v4.1.2/sql/catalyst/src/main/scala/org/apache/spark/sql/internal/StaticSQLConf.scala#L35) — `WAREHOUSE_PATH` default = `./spark-warehouse`
- [python/pyspark/sql/session.py:175](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/sql/session.py#L175) — Python `SparkSession` class
- [python/pyspark/sql/session.py:431](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/sql/session.py#L431) — Python `Builder.getOrCreate()`: Classic vs Connect routing
- [python/pyspark/sql/session.py:619](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/sql/session.py#L619) — Python `SparkSession.__init__`: Py4J bridge to JVM classic session
- [python/pyspark/sql/session.py:1230](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/sql/session.py#L1230) — `_create_shell_session()`: tries Hive, falls back to `_getActiveSessionOrCreate`
- [python/pyspark/shell.py:82](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/shell.py#L82) — REPL bootstrap: calls `_create_shell_session()`, binds `spark`, `sc`, `sql`, `sqlContext`
- [python/pyspark/core/context.py:546](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/core/context.py#L546) — Python `SparkContext.setLogLevel` → `self._jsc.setLogLevel(logLevel)` via Py4J

## Relevant configs

| Config | Default | Effect |
|---|---|---|
| `spark.app.name` | random UUID | Application name in Spark UI and logs |
| `spark.master` | (required) | Cluster URL; `local`, `local[N]`, `local[*]`, `local[N,M]`, `spark://host:port`, etc. |
| `spark.log.level` | None (uses log4j2 config) | Overrides Log4j2 root logger at `SparkContext` startup; syncs to executors if `spark.executor.allowSyncLogLevel=true` |
| `spark.sql.catalogImplementation` | `in-memory` | `hive` activates `HiveSessionStateBuilder` and connects to HMS |
| `spark.sql.warehouse.dir` | `./spark-warehouse` | Default location for managed tables |
| `spark.sql.session.timeZone` | JVM default timezone | Session-level timezone for datetime operations |
| `spark.pyspark.python` | None (uses `python3` on PATH) | Python executable used on executors |
| `spark.pyspark.driver.python` | None (falls back to `spark.pyspark.python`) | Python executable on the driver |
| `spark.python.worker.reuse` | `true` | Keeps Python worker processes alive across tasks; required for `ModelCache`, reduces startup cost |
| `spark.submit.deployMode` | `client` | `client` = driver runs locally; `cluster` = driver on a cluster node (non-local masters only) |
| `spark.executor.isolatedSessionCache.size` | 100 | Sessions for which executor-side per-session state is cached (Connect server mode) |
| `spark.executor.pyspark.memory` | None (unlimited) | Off-heap memory limit for Python workers on executors |
| `spark.api.mode` | auto-detected | `classic` or `connect`; selects which session implementation `SparkSession.builder` constructs |

## Chapter notes

Gap analysis against **spark-book Ch 04** (`spark-book/ch04-sparksession.md`).

### Already in spark-book ✅

- Builder pattern, `getOrCreate()` trap, static vs runtime configs, `spark.conf.isModifiable()` — well covered with working examples.
- `newSession()` for isolated namespaces sharing one `SparkContext` — covered with a Mermaid diagram.
- `local[*]` using `availableProcessors()` — in Exercise 3.
- Classic vs Connect split — covered; `spark.api.mode`, `SPARK_REMOTE`, `.remote()` builder all mentioned.

### Not in spark-book — needs addition ❌

- **`cloneSession()`** — `newSession()` is covered but `cloneSession()` is not. `cloneSession()` copies the parent's `SessionState` (configs, UDFs, `managedJobTags`) and forces eager init — the correct tool when an isolated child must inherit the parent's current config snapshot, not start from defaults.

- **`create()` method** — only `getOrCreate()` is shown. `create()` (added in 3.5.0) always forces a new session regardless of any active session — the right call in test harnesses that need guaranteed isolation.

- **`spark.log.level` config** — the chapter shows `spark.sparkContext.setLogLevel("WARN")` as the only mechanism. The config-based approach (`SparkConf.set("spark.log.level", "WARN")`) is preferable: it fires at `SparkContext` startup before any Spark logging and also propagates to executors if `spark.executor.allowSyncLogLevel` is enabled.

- **`local[N,M]` syntax** — `local[*]` and `local[2]` are in Exercise 3, but `local[N,M]` (N threads + M max task failures) is not mentioned. Useful context: this is the only way to test task-retry logic in local mode.

- **3-layer class split** — the chapter mentions classic vs Connect behaviorally but does not name the three layers: `sql.SparkSession` (abstract API), `sql.classic.SparkSession` (implementation), `sql.connect.SparkSession`. Worth one sentence so readers aren't confused when they see the source or stack traces.

- **REPL bootstrap** — `shell.py`'s `_create_shell_session()` that binds `spark`, `sc`, `sql`, `sqlContext` globals is not covered. Low priority, but useful for readers who use the `pyspark` shell or Jupyter.
