---
topic: B1
title: "Spark Architecture & the Execution Model"
status: complete
chapter: 3
repos: [apache/spark]
configs:
  - spark.driver.memory
  - spark.driver.cores
  - spark.driver.host
  - spark.driver.maxResultSize
  - spark.executor.memory
  - spark.executor.cores
  - spark.executor.instances
  - spark.executor.heartbeatInterval
  - spark.cores.max
  - spark.default.parallelism
  - spark.scheduler.mode
  - spark.scheduler.maxRegisteredResourcesWaitingTime
  - spark.submit.deployMode
  - spark.task.cpus
  - spark.task.maxFailures
  - spark.eventLog.enabled
  - spark.eventLog.dir
sources:
  - subsystem: core
    concepts: [SparkContext, DAGScheduler, TaskScheduler, Executor, LocalSchedulerBackend]
  - subsystem: python
    concepts: [PySpark bridge]
---

## What it is

Spark's execution model separates concerns across four tiers running in a single driver JVM plus one or more executor JVMs. The user-facing entry points are `SparkSession` (SQL/DataFrame API, wraps a `SparkContext`) and `SparkContext` (RDD API, owns all scheduler state). When an action is called, `SparkContext` delegates to `DAGScheduler`, which walks the RDD lineage graph, cuts it at every `ShuffleDependency` boundary to produce a DAG of `Stage` objects, and converts each ready stage into a `TaskSet`. `TaskSchedulerImpl` receives the `TaskSet`, wraps it in a `TaskSetManager` (which tracks per-task retries), and calls `backend.reviveOffers()` on its `SchedulerBackend`. The backend negotiates resources with the cluster manager, then calls `Executor.launchTask()` on each worker, which submits a `TaskRunner` to a thread pool. In local mode the driver, backend, and executor all share one JVM. For PySpark, a Python process communicates with the driver JVM via the Py4J gateway; Python worker processes are spawned per-task for RDD lambdas, but DataFrame operations are fully executed inside the JVM.

## Code path

```
SparkSession.builder.getOrCreate()
  → Builder.build()                                 [SparkSession.scala:979]
      checks active/default session, then:
  → SparkContext.<init>                             [SparkContext.scala:86]
      → createTaskScheduler(sc, master)             [SparkContext.scala:600]
            "local[*]"  → TaskSchedulerImpl + LocalSchedulerBackend
            "spark://…" → TaskSchedulerImpl + StandaloneSchedulerBackend
            other       → ExternalClusterManager via ServiceLoader
      → _dagScheduler = new DAGScheduler(sc)        [SparkContext.scala:603]
      → _taskScheduler.start()                      [SparkContext.scala:629]

--- action triggered (e.g. df.count()) ---

SparkContext.runJob(rdd, func, partitions)          [SparkContext.scala:2496]
  → DAGScheduler.runJob(...)                        [DAGScheduler.scala:997]
      → submitJob → eventProcessLoop.post(JobSubmitted)
  ↓ event loop receives JobSubmitted
  → handleJobSubmitted(...)                         [DAGScheduler.scala:1328]
      → createResultStage → getMissingParentStages  [DAGScheduler.scala:765]
            ShuffleDependency → new ShuffleMapStage
            NarrowDependency  → same stage (pipelined)
      → submitStage (recursive)                     [DAGScheduler.scala:1460]
      → submitMissingTasks → taskScheduler.submitTasks(TaskSet)  [DAGScheduler.scala:1747]

TaskSchedulerImpl.submitTasks(taskSet)             [TaskSchedulerImpl.scala:243]
  → TaskSetManager created, added to schedulableBuilder
  → backend.reviveOffers()                          [TaskSchedulerImpl.scala:284]

LocalSchedulerBackend.reviveOffers()               [LocalSchedulerBackend.scala:152]
  → LocalEndpoint.reviveOffers()                   [LocalSchedulerBackend.scala:91]
      → scheduler.resourceOffers(offers)            [TaskSchedulerImpl.scala:488]
      → executor.launchTask(executorBackend, task)  [LocalSchedulerBackend.scala:97]

Executor.launchTask(...)                           [Executor.scala:550]
  → new TaskRunner → threadPool.execute(tr)

TaskRunner.run()                                   [Executor.scala:785]
  → task.run(...)   (ShuffleMapTask writes; ResultTask calls user func)
  → execBackend.statusUpdate(FINISHED, result)

--- PySpark path ---

pyspark.core.context.SparkContext._ensure_initialized()  [context.py:451]
  → java_gateway.launch_gateway()                        [java_gateway.py:39]
      spawns JVM, reads back Py4J port, creates ClientServer
  → _initialize_context()                                [context.py:443]
      self._jvm.JavaSparkContext(jconf)  ← Py4J proxy call into JVM
```

## Key source locations

- [SparkSession.scala:92](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L92) — `class SparkSession` (in `sql.classic` package since Spark 4.x split); wraps a `SparkContext`
- [SparkSession.scala:979](https://github.com/apache/spark/blob/v4.1.2/sql/core/src/main/scala/org/apache/spark/sql/classic/SparkSession.scala#L979) — `Builder.build()`: checks active/default session before constructing a new `SparkContext`
- [SparkContext.scala:86](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/SparkContext.scala#L86) — `class SparkContext`: the single driver-side JVM entry point for all Spark functionality
- [SparkContext.scala:3288](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/SparkContext.scala#L3288) — `createTaskScheduler` factory: `"local"` → `LocalSchedulerBackend`; `"spark://…"` → `StandaloneSchedulerBackend`; anything else → `ExternalClusterManager` via `ServiceLoader`
- [DAGScheduler.scala:124](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/scheduler/DAGScheduler.scala#L124) — `class DAGScheduler`; scaladoc at L59–L122 is the most concise description of the stage/job/task model in the codebase
- [DAGScheduler.scala:765](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/scheduler/DAGScheduler.scala#L765) — `getMissingParentStages`: the exact function that mechanically enforces the narrow-vs-wide dependency split
- [DAGScheduler.scala:1460](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/scheduler/DAGScheduler.scala#L1460) — `submitStage`: recursive stage submission; missing parents → `waitingStages`; ready → `submitMissingTasks`
- [TaskSchedulerImpl.scala:243](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/scheduler/TaskSchedulerImpl.scala#L243) — `submitTasks`: creates `TaskSetManager`, registers with `schedulableBuilder`, calls `backend.reviveOffers()`
- [TaskSchedulerImpl.scala:488](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/scheduler/TaskSchedulerImpl.scala#L488) — `resourceOffers`: matches `WorkerOffer`s to pending tasks, enforces data locality levels
- [Executor.scala:249](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/executor/Executor.scala#L249) — `class Executor`: lives on the worker JVM (or driver JVM in local mode); owns thread pool and `BlockManager` reference
- [Executor.scala:666](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/executor/Executor.scala#L666) — `class TaskRunner`: deserialises `Task`, calls `task.run(...)`, sends result back via `execBackend.statusUpdate`
- [LocalSchedulerBackend.scala:50](https://github.com/apache/spark/blob/v4.1.2/core/src/main/scala/org/apache/spark/scheduler/local/LocalSchedulerBackend.scala#L50) — `class LocalEndpoint`: in-process RPC endpoint; directly holds an `Executor` so driver and executor share one JVM
- [java_gateway.py:39](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/java_gateway.py#L39) — `launch_gateway()`: spawns the JVM, reads back the Py4J connection port, creates `ClientServer`
- [context.py:443](https://github.com/apache/spark/blob/v4.1.2/python/pyspark/core/context.py#L443) — `_initialize_context()`: calls `self._jvm.JavaSparkContext(jconf)` via Py4J; the Python `SparkContext` is a thin proxy from this point

## Relevant configs

| Config | Default | Effect |
|---|---|---|
| `spark.driver.memory` | 1g | JVM heap for the driver process; must be set before JVM starts (`spark-submit --driver-memory`) |
| `spark.driver.cores` | 1 | Cores allocated to the driver in cluster deploy mode |
| `spark.driver.host` | local canonical hostname | Address advertised to executors for RPC; override when driver is behind NAT |
| `spark.driver.maxResultSize` | 1g | Cumulative size limit for task results collected at the driver; tasks exceeding this are dropped with an error |
| `spark.executor.memory` | 1g | JVM heap per executor; primary knob for out-of-memory tuning |
| `spark.executor.cores` | 1 | Task slots per executor; higher values share the JVM heap across more concurrent tasks |
| `spark.executor.instances` | (none) | Fixed executor count in static allocation; absent triggers dynamic allocation if enabled |
| `spark.executor.heartbeatInterval` | 10s | How often executors report metrics to the driver; must be less than `spark.network.timeout` |
| `spark.cores.max` | (none) | Standalone cluster only: max cores the application may claim across all executors |
| `spark.default.parallelism` | largest upstream partition count | Default partition count for shuffles and `sc.parallelize` |
| `spark.scheduler.mode` | FIFO | `FIFO` or `FAIR`; controls which `TaskSet` gets cores first when multiple stages compete |
| `spark.scheduler.maxRegisteredResourcesWaitingTime` | 30s | How long `TaskSchedulerImpl.postStartHook()` waits for executors before proceeding |
| `spark.submit.deployMode` | client | `client` (driver in submitting process) vs `cluster` (driver inside cluster) |
| `spark.task.cpus` | 1 | CPU slots consumed per task; increase for multi-threaded library tasks |
| `spark.task.maxFailures` | 4 | Times a single task may fail before the stage aborts; overridden to 1 in local mode |
| `spark.eventLog.enabled` | false | When true, writes `SparkListenerEvent`s for post-mortem replay in the History Server |
| `spark.eventLog.dir` | /tmp/spark-events | Destination for event log files (local or HDFS) |

## Chapter notes

Gap analysis against **spark-book Ch 01–03** (`ch01-introduction-to-spark.md`, `ch02-spark-architecture.md`, `ch03-spark-installation.md`).

### Already in spark-book ✅

- Driver/executor/cluster-manager model, Py4J bridge, local mode = one JVM — all in Ch02 with diagrams.
- DAGScheduler single-threaded event loop — explicitly stated in Ch02: *"All these decisions are driven by a single-threaded event loop."*
- Narrow vs wide dependencies, stage boundaries, ShuffleMapStage/ResultStage — covered in Ch02.
- Lazy evaluation (transformation vs action), QueryExecution pipeline — covered in Ch02.
- Fault tolerance via lineage, speculative execution — covered in Ch02.
- `spark.task.maxFailures` (default 4) and task/stage failure handling — covered in Ch02.
- **`sql.classic` package split** — added to Ch02 SparkSession section: `SparkSession` in Spark 4.x lives in `org.apache.spark.sql.classic`, coexisting with `org.apache.spark.sql.connect.SparkSession`; Python import unchanged.
- **`spark.task.maxFailures` overridden to 1 in local mode** — added to Ch02 Failure handling section: `TaskSchedulerImpl` is constructed with `maxTaskFailures = 1` in local mode regardless of config; a single task failure aborts the stage.
- **`getMissingParentStages` as the single enforcement point** — added to Ch02 Narrow and wide dependencies section with source link to DAGScheduler.scala:765.
- **PySpark two-channel bridge** — added to Ch02 Driver Program section: Py4J (Python→JVM) vs `pyspark.worker` socket (JVM→Python for UDFs/RDD lambdas) are separate channels; DataFrame built-ins never cross to Python.

### Not in spark-book — needs addition ❌

— all gaps resolved as of 2026-06-06.
